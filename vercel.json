{
	"version": 2,
	"builds": [
		{
			"src": "backend/index.py",
			"use": "@vercel/python"
		}
	],
	"buildCommand": "cd frontend && npm run build",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/backend/index.py"
		}
	]
}
